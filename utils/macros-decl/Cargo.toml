[package]
name = "macros-decl"
version = "0.1.0"
authors = ["Remco Bloemen <remco@0x.org>"]
edition = "2018"

[dependencies]
macros-impl = { path = "../macros-impl" }

proc-macro-hack = "0.5"

[features]
default = ["std"]
std = []
