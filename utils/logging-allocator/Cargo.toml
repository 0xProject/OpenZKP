[package]
name = "logging-allocator"
version = "0.1.0"
authors = ["Remco Bloemen <remco@0x.org>"]
edition = "2018"

[dependencies]
log = { version = "0.4.8", default_features = false }

[features]
default = [ "std"]
std = [
    "log/std",
]
